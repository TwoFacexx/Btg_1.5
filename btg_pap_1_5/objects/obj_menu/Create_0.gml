menu_inicial = ["Jogar", "Opções", "Sair"];
esc = []; // Inicializando esc como array vazio

// Inicializa o array esc com valores 1
for (var i = 0; i < array_length(menu_inicial); i++) {
    esc[i] = 1;
}
