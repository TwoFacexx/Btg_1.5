// Limpar surface se existir  
// Clear surface if it exists  
if (surface_exists(surf_pause)) {
    surface_free(surf_pause);
    // pt-PT: Liberta a memória da surface de pausa se estiver criada  
    // EN: Frees the memory of the pause surface if it was created
}
