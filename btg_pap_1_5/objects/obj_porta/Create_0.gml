estado = "fechando"; //idle, abrindo, fechando
// Define o estado atual como "fechando" (porta a fechar)
// Define the current state as "closing" (door is closing)

sprite_index = spr_porta_fecha;
// Altera o sprite para a animação de porta fechando
// Changes the sprite to the door closing animation

image_speed = 0;
// Pausa a animação (imagem estática)
// Pauses the animation (static image)
